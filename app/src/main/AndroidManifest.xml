<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.drawabletest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ClipDrawableActivity"></activity>
        <activity android:name=".activity.ScaleDrawableActivity" />
        <activity android:name=".activity.InsetDrawableActivity" />
        <activity android:name=".activity.TransitionDrawableActivity" />
        <activity android:name=".activity.LevelListDrawableActivity" />
        <activity android:name=".activity.StateListDrawableActivity" />
        <activity android:name=".activity.LayerDrawableActivity" />
        <activity
            android:name=".activity.ShapeDrawableActivity"
            android:label="ShapeDrawable" />
        <activity
            android:name=".activity.NinePatchDrawableActivity"
            android:label="NinePatchDrawableActivity" />
        <activity
            android:name=".activity.BitmapDrawableActivity"
            android:label="BitmapDrawableActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>